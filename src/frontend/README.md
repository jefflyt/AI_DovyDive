# Frontend (Next.js)

Purpose: Next.js frontend (App Router, TypeScript) using Tailwind CSS.

Suggested structure:
- `app` - Next.js app routes
- `components` - UI components

Quick start (dev):
- Install dependencies: `npm install`
- Run: `npm run dev` (or `next dev`)
