To scaffold the frontend quickly:

1. Run `npx create-next-app@latest src/frontend --typescript --src-dir --app` from the repo root.
2. Install Tailwind and setup per Next.js docs.
3. Move the generated files into `src/frontend` if needed.

